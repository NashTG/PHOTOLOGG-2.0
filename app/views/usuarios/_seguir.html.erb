<%= form_for([@usuario,@amigo]) do |f| %>
     <% f.number_field :seguidor,class:"form-control" , :value => current_usuario.id%>

	<% f.number_field :seguido,class:"form-control" , :value => @usuario.id%>

  	
  <%= f.submit "Seguir", class: "btn btn-primary" %>
<% end %>